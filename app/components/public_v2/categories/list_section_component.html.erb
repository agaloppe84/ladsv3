<%= tag.section id: "produits", class: component_classes, data: debug_data, aria: { label: "Produits par categorie" } do %>
  <% sections.each do |section| %>
    <%= render PublicV2::Categories::ProductSliderSectionComponent.new(section: section, debug: debug?) %>
  <% end %>
<% end %>
