<div class="flex space-x-4 items-center">
  <% @motorists.each do |motorist| %>
    <div class="inline-flex items-center justify-center rounded-lg bg-slate-50 px-3 py-1 text-xs font-medium text-slate-700 ring-1 ring-inset ring-slate-700/10 mt-4">
      <%= inline_svg_tag("logos/#{motorist.logo}", class: "fill-slate-700 h-10 w-20") %>
    </div>
  <% end %>
</div>